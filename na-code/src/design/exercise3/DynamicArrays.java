package design.exercise3;

/**
 * 3-3
 * Arrays both grow and shrink.
 * --- if shrink in half when the number of element is below half full of the array;
 * suppose we have an array start with half full, now we delete one element, it
 * shrinks to half; if now we insert one element, it will extend the array size again.
 * --- if shrink something like 1/4 size, we give the array a buffer for the possible
 * size decrease and increase.
 * 
 * @author nz026920
 * 
 */
public class DynamicArrays {

}
